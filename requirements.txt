# Runtime dependencies for the AI Analytics Copilot.
#
# Requires Python >=3.11,<3.14 — every published version of `unstructured`
# caps below 3.14. See .python-version.
#
# This file lists direct imports only. It replaces a `pip freeze` dump that
# could not install anywhere: it pinned `en-core-web-sm`, a spaCy model that
# has never been published to PyPI.
#
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt   # tests

# LLM + embeddings (one HuggingFace provider for SQL, RAG, and routing)
huggingface-hub>=1.15
sentence-transformers>=5.5

# LangChain pieces actually imported by src/
langchain-core>=1.4
langchain-community>=0.4          # BM25Retriever (src/rag/retrieval.py)
langchain-chroma>=1.1
langchain-huggingface>=1.2
langchain-text-splitters>=1.1
chromadb>=1.5
rank-bm25>=0.2.2                  # required by BM25Retriever, not pulled in automatically

# PDF ingestion for the report corpus
unstructured[pdf]>=0.22

# Data handling
pandas>=3.0
numpy>=2.4
pydantic>=2.13

# Dashboard UI (app/dashboard.py); altair ships with streamlit
streamlit>=1.57

# Configuration and data provisioning
python-dotenv>=1.2
kagglehub>=1.0                    # scripts/build_db.py
